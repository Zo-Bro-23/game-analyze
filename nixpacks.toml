[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip', 'ffmpeg', 'libGL', 'libglib', 'zlib', 'libjpeg', 'libpng']

[phases.install]
cmds = ['pip install -r requirements_web.txt']

[phases.build]
cmds = []

[start]
cmd = 'python -m gunicorn web_app_backend:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120'

